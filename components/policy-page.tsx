export function PolicyPage({title,intro,sections}:{title:string;intro:string;sections:string[]}){ return <div className="site-container section-space max-w-4xl"><p className="text-xs font-semibold uppercase tracking-[.28em] text-primary">VIGNESHWARI Care</p><h1 className="mt-4 text-6xl">{title}</h1><p className="mt-6 max-w-2xl text-lg leading-relaxed text-muted-foreground">{intro}</p><div className="mt-14 flex flex-col gap-10">{sections.map((section,index)=><section key={section} className="border-t border-border pt-6"><h2 className="text-3xl">{index+1}. {section}</h2><p className="mt-3 leading-relaxed text-muted-foreground">This content is a considered placeholder for Phase 1. Final operational details will be reviewed and published before the custom ecommerce platform accepts customer information or orders.</p></section>)}</div></div> }
