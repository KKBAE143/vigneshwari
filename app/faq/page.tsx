import type { Metadata } from 'next'
import { Accordion, AccordionContent, AccordionItem, AccordionTrigger } from '@/components/ui/accordion'
export const metadata: Metadata = { title: 'Frequently Asked Questions' }
const faqs=[['Are the sarees handwoven?','Our Phase 1 catalog is illustrative. The future platform will show verified weave, artisan, and provenance details for every piece.'],['Can I place an order now?','Not yet. This is a frontend-only preview and checkout will arrive with the custom commerce backend in Phase 2.'],['How should I care for silk?','Store silk wrapped in muslin, air it periodically, and use a trusted specialist dry cleaner.'],['What is your return policy?','The return flow is a visual placeholder. Final terms will be published before commerce launches.'],['Do you offer bridal appointments?','The appointment UI will connect to our service system in Phase 2.']]
export default function FaqPage(){ return <div className="site-container section-space max-w-4xl"><div className="mb-12 text-center"><p className="text-xs font-semibold uppercase tracking-[.28em] text-primary">At your service</p><h1 className="mt-4 text-6xl">Frequently asked</h1></div><Accordion>{faqs.map(([q,a],i)=><AccordionItem key={q} value={`${i}`}><AccordionTrigger className="font-serif text-2xl">{q}</AccordionTrigger><AccordionContent className="leading-relaxed text-muted-foreground">{a}</AccordionContent></AccordionItem>)}</Accordion></div> }
